# frozen_string_literal: true

RSpec.describe SummaryAction do
  it "has a version number" do
    expect(SummaryAction::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
